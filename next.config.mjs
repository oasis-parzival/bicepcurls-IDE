/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/bicepcurls-IDE',
    assetPrefix: '/bicepcurls-IDE/',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
